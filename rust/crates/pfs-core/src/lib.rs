//! pfs-core — voir le Plan Directeur v2.0 §2.2 pour la spécification du module.
//!
//! ATTENTION licence : ce crate est MIT. Ne JAMAIS lier de code AGPL-3.0
//! (postflop-solver, TexasSolver, DecisionHoldem) — le copyleft se propage
//! au travers du linking statique Rust.

#![forbid(unsafe_op_in_unsafe_fn)]

#[cfg(test)]
mod tests {
    #[test]
    fn phase0_placeholder() {
        assert!(true);
    }
}
